# Backend API base URL
# Local dev — leave empty (Vite proxy handles /api -> localhost:3001)
VITE_API_BASE_URL=

# Production — set to your Render backend URL in Netlify environment variables
# VITE_API_BASE_URL=https://fittrack-v0fi.onrender.com/api
